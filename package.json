{
  "name": "snake-game",
  "version": "1.1.0",
  "private": true,
  "description": "Snake vs Rats with optional two-player mode",
  "license": "UNLICENSED",
  "scripts": {
    "dev": "npm install && npm exec -y -- nodemon --watch server.js --watch index.html --watch main.js --watch style.css --ext js,html,css --exec \"node server.js\"",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "nanoid": "^5.0.7",
    "ws": "^8.18.0"
  }
}
